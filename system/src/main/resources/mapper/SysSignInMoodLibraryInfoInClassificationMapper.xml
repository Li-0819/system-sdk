<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iris.mapper.ISysSignInMoodLibraryInfoInClassificationMapper">

    <insert id="commonAddInfoInClassify">

        insert into sys_sign_in_mood_library_info_in_classification (id, classify_id, mood_library_info_id)
        value
        <foreach collection="classifyIds" item="classifyId" separator="," index="index">
        (
            md5(replace(uuid(), '-', '')),
            #{classifyId, jdbcType=VARCHAR},
            #{id, jdbcType=VARCHAR}
        )

        </foreach>
    </insert>
</mapper>